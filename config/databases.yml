all:
  propel:
    class:        sfPropelDatabase
    param:
      phptype:    mysql
      host:       localhost

      # NOTE: if you change this name update also:
      #   * config/propel.ini
      #   * scripts/initdb.sh
      #
      database:   asterisell3
      username:   root
      password:   root
      encoding:   utf8
