<?php
use_helper('Markdown');
// Default culture is en_US
//
echo insertHelp('

## Parameters

This rate has no parameters, because all his parameters are built-in inside the code.

Inspect the rate source code inside `apps/asterisell/lib/rates` for updating it.

');
?>